package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Alahmadi Brand Colors - Geometric Balance Theme (Deep Bluish Teal & Golden Accent)
val TealDark = Color(0xFF081F24)       // Deep background base from design HTML
val TealMedium = Color(0xFF0B2830)     // Navigation surface background from design HTML
val TealLight = Color(0xFF13404A)      // Light teal gradient stop / surfaces
val GoldAccent = Color(0xFFFFD700)     // Golden yellow brand color from design HTML
val GoldAccentLight = Color(0xFFFFE066)// Light golden yellow
val NeutralLight = Color(0xFFF4F6F7)   // Clean light neutral
val TextWhite = Color(0xFFFFFFFF)      // White text
val TextGray = Color(0xFFB0BEC5)       // Gray text
val CardBackground = Color(0xFF0F353E) // Dark teal card background to match geometric surfaces
val AlertError = Color(0xFFEF4444)     // Bright red for error/failed status (Tailwind style)
val AlertSuccess = Color(0xFF10B981)   // Green for success status (Tailwind style)
val AlertWarning = Color(0xFFF59E0B)   // Amber/orange for waiting/preparing status (Tailwind style)
val AlertInfo = Color(0xFF3B82F6)      // Blue for shipping status (Tailwind style)
val ShimmerBase = Color(0xFF0E3841)    // Base color for skeleton loader
